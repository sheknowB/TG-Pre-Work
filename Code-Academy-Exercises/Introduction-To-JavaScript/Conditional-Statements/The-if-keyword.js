let sale = true;
sale = false;
if (sale) {
  console.log('Time to buy!')
} // Prints "Time to buy!"

/* We learn the structure of an if statement. It is comprised of the if keyword, followed by a set of parantheses, followed by a code block.
  A condition would be set in the parantheses, and the code block would contain a statement or a task to be performed. */
  
