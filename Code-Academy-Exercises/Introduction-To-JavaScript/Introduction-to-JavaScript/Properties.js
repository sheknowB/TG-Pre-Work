console.log('Teaching the world how to code'.length);

/* When we introduce a new piece of data into a JavaScript program, the browser saves it as an instance of the data type.
  These pieces of data comes with properties. You can retrieve property information by appending a period followed by the property name.
  The . acts as an operator called a dot operator. In this exercise we are retrieving the length of a string.
*/   
