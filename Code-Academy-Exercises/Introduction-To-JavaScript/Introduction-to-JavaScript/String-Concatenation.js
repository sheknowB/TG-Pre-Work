console.log('Hello ' + 'World');

/* String concatenation appends strings together. We use the + operator to attach strings together.
  The output of the concatenation will appear exactly as the string is written. */
