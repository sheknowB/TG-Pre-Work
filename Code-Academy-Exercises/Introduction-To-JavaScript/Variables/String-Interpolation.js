var myName = 'Brandon'; //Declared variable named myName.
var myCity = 'Honolulu'; //Declared variable named myCity
console.log(`My name is ${myName}. My favorite city is ${myCity}`); //Output: My name is Brandon. My favorite city is Honolulu.

/* We can insert variables into strings using template literals. A template literal is wrapped in backticks (`).
  Placeholder looks like this ${variable}. */
