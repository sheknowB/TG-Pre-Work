const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight';
  console.log(lightWaves);
}
logVisibleLightWaves();
//Prints Moonlight
console.log(lightWaves);
//ReferenceError

/* When a variable is defined in a block, it is only accessible to the code within the cury braces {}.
  We call these block scope variables because it is only accessible to the lines of code within that block. These are known as local variables.
*/
